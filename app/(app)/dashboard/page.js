export const dynamic = "force-dynamic";

export default async function DashboardPage() {
  return (
    <div>
      <h2 className="font-display text-2xl font-semibold mb-1">Diagnostic Dashboard</h2>
      <p className="text-slate text-sm mb-5">Minimal version — testing whether the crash is inside this page.</p>
    </div>
  );
}
